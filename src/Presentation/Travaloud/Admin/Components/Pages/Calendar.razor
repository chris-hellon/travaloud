@page "/calendar"


@attribute [MustHavePermission(TravaloudAction.View, TravaloudResource.Calendar)]

@inject IStringLocalizer<Dashboard> L

<TravaloudPageTitle Title="@L["Calendar"]"/>

<MudGrid>
    <MudItem xs="12" sm="12" md="12">
        <MudCalendar Items="CalendarItems" MonthCellMinHeight="100" DateRangeChanged="DateRangeChanged" ItemClicked="OnItemClick">
            <CellTemplate>
                <MudTooltip Inline="false" RootStyle="width:100%; height:100%;">
                    <ChildContent>
                        <MudChip Color="Color.Primary" Label="true" Class="mud-cal-cell-template-chip" Style="margin-bottom:1px;">@context.Text.Split('|')[0]</MudChip>
                    </ChildContent>
                    <TooltipContent>
                        <MudText Typo="Typo.h6">@context.Text.Split('|')[0]</MudText>
                        <MudText Typo="Typo.body2">@context.Text.Split('|')[3]</MudText>
                        <MudText Typo="Typo.body2">@context.Text.Split('|')[1]</MudText>
                    </TooltipContent>
                </MudTooltip>
            </CellTemplate>
        </MudCalendar>
    </MudItem>
</MudGrid>